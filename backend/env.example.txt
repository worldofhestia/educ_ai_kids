# EducAIKids Backend Environment Variables
# Renommer ce fichier en .env et remplir les valeurs

# Application
DEBUG=false
APP_NAME=EducAIKids
APP_VERSION=1.0.0

# Server
HOST=0.0.0.0
PORT=8000

# OpenRouter (LLM) - https://openrouter.ai/
OPENROUTER_API_KEY=your_openrouter_api_key_here
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
OPENROUTER_MODEL=anthropic/claude-3.5-sonnet

# Replicate - https://replicate.com/
REPLICATE_API_TOKEN=your_replicate_api_token_here

# Modèles Replicate (optionnel - valeurs par défaut utilisées si non spécifié)
REPLICATE_IMAGE_MODEL=black-forest-labs/flux-schnell
# TTS: kokoro (défaut), parler, styletts2, bark - Le système essaie automatiquement
REPLICATE_TTS_MODEL=minimax/speech-02-turbo
REPLICATE_MUSIC_MODEL=meta/musicgen
REPLICATE_VIDEO_MODEL=wan-video/wan2.1-i2v-480p-14b

# Redis (pour tâches asynchrones avancées)
REDIS_URL=redis://localhost:6379/0

# Dossiers
OUTPUT_DIR=./outputs
TEMP_DIR=./temp

# Paramètres de génération
MAX_SCENES=10
SCENE_DURATION_MIN=3.0
SCENE_DURATION_MAX=5.0
VIDEO_RESOLUTION=480p

# CORS (origines autorisées, séparées par des virgules)
CORS_ORIGINS=["http://localhost:3000","http://127.0.0.1:3000"]

